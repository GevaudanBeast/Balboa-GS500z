{
  "config": {
    "step": {
      "user": {
        "title": "Setup Balboa GS500Z Spa",
        "description": "This integration provides READ-ONLY monitoring via RS-485 (temperature, mode, heater status). Control requires IR module + ESP32 (see documentation). Enter your EW11A module IP address and port.",
        "data": {
          "host": "IP Address (e.g., 192.168.1.100)",
          "port": "TCP Port"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to EW11A module. Check: (1) IP address is correct, (2) Port is 8899, (3) Module is powered on and connected to WiFi."
    },
    "abort": {
      "already_configured": "This device is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Balboa GS500Z Options",
        "description": "Configure advanced options. Window: larger = more reliable but slower updates (recommended: 5). Guard: prevents invalid mode transitions (recommended: enabled).",
        "data": {
          "window_size": "Data Validation Window (3-20 frames)",
          "order_guard": "Enforce Safe Mode Transitions"
        }
      }
    },
    "error": {
      "window_size_too_small": "Window size must be at least 3 frames for reliable validation",
      "window_size_too_large": "Window size cannot exceed 20 frames (would be too slow)"
    }
  },
  "entity": {
    "climate": {
      "spa": {
        "name": "Spa"
      }
    },
    "binary_sensor": {
      "spa_heater": {
        "name": "Heater"
      }
    }
  }
}
